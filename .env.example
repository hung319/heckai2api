# Cổng chạy server (Mặc định 3000)
PORT=3000

# Khóa bảo mật (Bearer Token)
API_MASTER_KEY=sk-heck2api-secret-key

# URL Upstream (Giữ nguyên nếu không đổi)
UPSTREAM_API_BASE=https://api.heckai.weight-wave.com/api/ha/v1
