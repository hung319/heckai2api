# Cổng chạy service (Mặc định 3000)
PORT=3000

# Khóa bảo vệ API (Bearer Token)
API_MASTER_KEY=sk-heck-secret-key-123

# Cấu hình Upstream (Giữ nguyên nếu không đổi)
UPSTREAM_API_BASE=https://api.heckai.weight-wave.com/api/ha/v1
